app_identifier("ch.miniapps.wealthtracker")
apple_id(ENV['APPLE_ID'] || "your-apple-id@example.com")
team_id(ENV['TEAM_ID'] || "7PSHT7LSVZ")

itc_team_id(ENV['ITC_TEAM_ID'])

for_platform :ios do
  for_lane :beta do
    app_identifier("ch.miniapps.wealthtracker")
  end

  for_lane :release do
    app_identifier("ch.miniapps.wealthtracker")
  end
end
