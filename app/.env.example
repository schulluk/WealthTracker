# =============================================================================
# iOS App Store Connect
# =============================================================================

# Your Apple ID email used for App Store Connect
# Get from: https://appleid.apple.com
APPLE_ID=your-apple-id@example.com

# Apple Developer Team ID (10-character string)
# Get from: https://developer.apple.com/account → Membership → Team ID
TEAM_ID=

# App Store Connect Team ID (numeric)
# Usually NOT needed - fastlane auto-selects if you only have one team
# Only set this if fastlane prompts you to choose between multiple teams
ITC_TEAM_ID=

# =============================================================================
# App Store Connect API Key (REQUIRED for command-line builds)
# =============================================================================
# This is needed because xcodebuild can't access Xcode's saved accounts.
#
# How to create:
#   1. Go to: https://appstoreconnect.apple.com/access/integrations/api
#   2. Click "Generate API Key" (or "+" if you have existing keys)
#   3. Enter a name (e.g., "Fastlane") and select "Admin" access
#   4. Download the .p8 file (you can only download it ONCE!)
#   5. Save the .p8 file to ios/fastlane/AuthKey_XXXXXX.p8
#   6. Note the Key ID and Issuer ID shown on the page

# Key ID (10-character string, shown in App Store Connect)
ASC_KEY_ID=

# Issuer ID (UUID, shown at the top of the API Keys page)
ASC_ISSUER_ID=

# Path to the .p8 key file (relative to app/ directory)
ASC_KEY_PATH=ios/fastlane/AuthKey.p8

# =============================================================================
# Android Play Store
# =============================================================================

# Path to Google Play Store service account JSON key file
# How to create:
#   1. Create a Google Cloud project: https://console.cloud.google.com/projectcreate
#   2. Enable the Google Play Developer API for your project:
#      https://console.cloud.google.com/apis/library/androidpublisher.googleapis.com
#   3. Create a service account:
#      https://console.cloud.google.com/iam-admin/serviceaccounts → Create Service Account
#      - Enter a name (e.g., "fastlane-deploy")
#      - Skip "Grant this service account access" (no Cloud IAM roles needed)
#      - Skip "Grant users access" → Done
#   4. Create a JSON key for the service account:
#      Click the service account → Keys → Add Key → Create new key → JSON
#   5. In Play Console, invite the service account:
#      https://play.google.com/console → Users & Permissions → Invite new users
#      Enter the service account email (ends with @*.iam.gserviceaccount.com)
#      Grant "Release apps to testing tracks" and "Release to production" permissions
#   6. Wait up to 24 hours for activation, then save the JSON key file
# Path is relative to android/ directory (release.sh cd's into android/ before running fastlane)
PLAY_STORE_JSON_KEY=fastlane/play-store-key.json
